# from flask_wtf import FlaskForm
# from wtforms import BooleanField
# from wtforms.validators import DataRequired

# class CreateBookmark(FlaskForm):
#   bookmarked = BooleanField('Bookmark', validators=[DataRequired()])

#let's not use this for now, so don't put it in __init